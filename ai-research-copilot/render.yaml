services:
  # PostgreSQL Database
  - type: postgresql
    name: airc-database
    plan: free
    databaseName: airc_db
    user: airc_user

  # Backend API
  - type: web
    name: airc-backend
    plan: free
    runtime: docker
    dockerfilePath: ./docker/backend/Dockerfile.render
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: LOG_LEVEL
        value: info
      - key: DATABASE_URL
        fromDatabase:
          name: airc-database
          property: connectionString
      - key: REDIS_URL
        sync: false
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        value: 'https://abryx-ai.vercel.app,https://*.vercel.app,http://localhost:3000'
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 8000
      - key: WORKERS
        value: 2
      - key: ENCRYPTION_KEY
        generateValue: true
      # OAuth redirect URLs (CRITICAL for production OAuth)
      - key: FRONTEND_URL
        value: "https://abryx-ai.vercel.app"
      - key: BACKEND_URL
        value: "https://agentic-ai-f27z.onrender.com"
      # Google OAuth
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      # GitHub OAuth
      - key: GITHUB_CLIENT_ID
        sync: false
      - key: GITHUB_CLIENT_SECRET
        sync: false
    healthCheckPath: /health
    autoDeploy: true

  # MCP Server
  - type: web
    name: airc-mcp-server
    plan: free
    runtime: docker
    dockerfilePath: ./docker/mcp/Dockerfile.render
    envVars:
      - key: APP_ENV
        value: production
      - key: LOG_LEVEL
        value: info
      - key: MCP_PORT
        value: 8100
      - key: MCP_SECRET_KEY
        generateValue: true
      - key: MCP_RATE_LIMIT_ENABLED
        value: "true"
      - key: MCP_RATE_LIMIT_MAX_REQUESTS
        value: "100"
      - key: MCP_RATE_LIMIT_WINDOW_SECONDS
        value: "60"
      - key: MCP_TIMEOUT
        value: "30"
      - key: MCP_MAX_RETRIES
        value: "3"
      - key: MCP_LOG_FORMAT
        value: json
    healthCheckPath: /health
    autoDeploy: true
